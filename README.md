# Content Insights

Monorepo root for Content Insights.

## Workspaces

- `frontend` - Next.js frontend app

## Commands

Run frontend commands from the repo root:

```bash
npm run dev
npm run build
npm run start
```

You can also run commands directly inside `frontend`:

```bash
cd frontend
npm run dev
```
